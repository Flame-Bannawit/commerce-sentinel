# ============================================
# Test Configuration
# ============================================

# API Base URL (override via env var in CI)
api.base.url=https://fakestoreapi.com
api.timeout.seconds=10

# E2E Browser (chrome | firefox | edge)
browser=chrome
browser.headless=true
browser.timeout.seconds=15

# Test Data
test.user.email=testuser@portfolio.dev
test.user.password=Test@1234

# Reporting
allure.results.directory=allure-results
screenshot.on.failure=true
